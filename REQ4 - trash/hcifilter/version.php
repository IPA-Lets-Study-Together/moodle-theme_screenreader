<?php
$plugin->component    = 'local_hcifilter'; // It may seem redundant to include the component name, since this can be deduced from the path, but the installer uses this to verify that the plugin has been installed in the right place
$plugin->version      = 2014060500;
$plugin->requires     = 2013010100;
$plugin->maturity     = MATURITY_STABLE; // ALPHA, BETA, RC (release candidate), or STABLE